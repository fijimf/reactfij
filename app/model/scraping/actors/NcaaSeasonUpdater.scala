package model.scraping.actors

case class TeamMap(keys:Set[String], data:Map[String,Any])


class NcaaSeasonUpdater extends Actor {

  def receive {
      
  
  
  }
  private def loadTeams = {
     
}
